@model IEnumerable<Map_Relationships.Models.Book>

@{
    ViewBag.Title = "MyIndex";
}

<h2>MyIndex</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ISBN)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ISBN)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Pk_Book_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Pk_Book_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Pk_Book_Id })
        </td>
    </tr>
}

</table>
